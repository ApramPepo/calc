#include <stdio.h> 

int main(void)
{
    char *oper;
    double a;
    double b;

    while (1)
    {
        printf("Please enter math Operator using (+ or -) only: ");
        scanf("%s", *oper);
        if (oper != "+", "-")
        {
            break;
            printf("invalid");
        }

        if (oper == "+")
        {

        }
    }
}